## Universal Memory

Before durable planning, implementation, investigation, or review, run
`umem status --format json` and `umem context --scope project --format json`.
Reconcile memory with the current repository and the user's instructions. Follow the
`universal-memory` Agent Skill when available. Persist only stable, reusable, and safe
information. If UMEM is unavailable, say so and continue without inventing context.
